{
  "name": "palette-sandbox",
  "version": "1.0.0",
  "description": "A sandbox for testing micro-UX enhancements.",
  "private": true,
  "scripts": {
    "lint": "echo 'No linter configured.'",
    "test": "echo 'No tests configured.'"
  }
}
